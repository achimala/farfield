{
  "name": "codex-companion",
  "version": "0.1.0",
  "private": true,
  "description": "Local web dashboard for Codex threads, turns, and advanced IPC controls",
  "scripts": {
    "build:ui": "node scripts/build-ui.mjs",
    "start": "npm run build:ui && node server.js"
  },
  "dependencies": {
    "esbuild": "^0.27.3",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
